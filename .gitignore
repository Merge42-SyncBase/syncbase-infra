.env
# The retired credential bootstrap is forbidden by the protected-material
# policy. Keep it ignored as defense in depth against accidental restaging.
bootstrap-shared-env.sh
environments/*/.env
environments/*/secrets/
build/
output/
__pycache__/
*.py[cod]
*.tfstate
*.tfstate.*

# Custom
.playwright-cli
PRODUCT.md

# Local retrieval calibration is deliberately not release evidence.
evaluation/runtime/
